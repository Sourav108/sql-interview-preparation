# Project 05: Multi-Tenant SaaS Subscription Engine

Subscription lifecycle management and automated invoice generation schema.

## Quickstart
```bash
psql -U postgres -d sql_interview_db < schema.sql
psql -U postgres -d sql_interview_db < seed.sql
psql -U postgres -d sql_interview_db < queries.sql
```
