# Project 03: Product Analytics & Telemetry Engine

Telemetry event logging database supporting sessionization, funnel conversion, and retention analytics.

## Quickstart
```bash
psql -U postgres -d sql_interview_db < schema.sql
psql -U postgres -d sql_interview_db < seed.sql
psql -U postgres -d sql_interview_db < queries.sql
```
